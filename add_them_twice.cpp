#include<bits/stdc++.h>
using namespace std;
int main()
{
    //freopen("input.txt", "r", stdin);
    //freopen("output.txt", "w", stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    double a, b, c, sum;
    cin >> a >> b >> c;
    sum = (a+a) + (b+b) + (c+c);
    cout << sum << "\n";
    return 0;
}
